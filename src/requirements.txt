# Lambda Runtime Dependencies
#
# Note: boto3 and botocore are included in AWS Lambda Python runtime.
# However, we need specific versions for Bedrock Agent Runtime support.
#
# Bedrock Agent Runtime support (requires boto3 >= 1.34.0)
boto3>=1.34.0
botocore>=1.34.0

# For development/testing dependencies, see ../requirements-dev.txt
