# Lambda Runtime Dependencies
#
# Note: boto3 and botocore are already included in AWS Lambda Python 3.12 runtime.
# This file is kept empty as no additional runtime dependencies are currently needed.
#
# Add packages here when your Lambda code needs them:
# requests>=2.31.0
# pydantic>=2.0.0
# pandas>=2.0.0
#
# For development/testing dependencies, see ../requirements-dev.txt
