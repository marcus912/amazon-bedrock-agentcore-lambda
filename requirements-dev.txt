# Development and Testing Dependencies
# Install with: pip install -r requirements-dev.txt

# Testing framework
pytest>=7.4.0
pytest-mock>=3.11.0

# AWS SDK (needed for local testing, already included in Lambda runtime)
boto3>=1.34.0
botocore>=1.34.0

# Note: aws-sam-cli is typically installed globally
# If you need it in a virtual environment, uncomment:
# aws-sam-cli>=1.100.0
